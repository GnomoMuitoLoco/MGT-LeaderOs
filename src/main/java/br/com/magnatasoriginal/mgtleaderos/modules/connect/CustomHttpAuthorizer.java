package br.com.magnatasoriginal.mgtleaderos.modules.connect;

/**
 * Stub para CustomHttpAuthorizer —
 * No projeto atual estamos usando o cliente WebSocket simples (SocketClient/LeaderOSSocketClient)
 * que já realiza a autenticação via HTTP manualmente. A implementação do cliente oficial
 * Pusher (com.pusher.client.*) é opcional; se desejarmos reativá-la, substitua este stub
 * pela implementação original e adicione a dependência adequada.
 *
 * // TODO (PT-BR): Implementar ou habilitar cliente Pusher se for necessário. No momento
 * mantemos um stub para compilar sem a biblioteca do Pusher.
 */
public class CustomHttpAuthorizer {
    // Classe intencionalmente vazia — funcionalidade Pusher não necessária para o fluxo atual.
}
